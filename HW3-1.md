# HW3-1：Naive DQN 與 Experience Replay

> **環境**：4×4 Gridworld　**實驗**：naive_static（靜態地圖）、replay_random（隨機地圖）

---

## 演算法說明

### Naive DQN

最基礎的 DQN 實現。每一步直接拿當下的 `(s, a, r, s')` 算 TD loss 並更新網路，沒有 replay buffer、沒有 target network。

**Bellman target：**

```
Y = r + γ · max_a' Q(s', a'; θ)
Loss = MSE(Q(s, a; θ), Y)
```

這裡的 target 和 online network 共享同一組參數 θ，每步更新都會改變 Y 本身，訓練信號不穩定。

**ε-greedy 探索採 linear decay：**

```
ε(t) = max(ε_end, ε_start − (ε_start − ε_end) × t / epochs)
```

從 1.0 線性退火到 0.1，讓訓練前期充分探索，後期收斂到貪心策略。

---

### DQN with Experience Replay

引入 replay buffer（容量 1000）和 target network，解決 Naive DQN 的兩個問題：

| 問題 | 解法 |
|------|------|
| 相鄰樣本高度相關，梯度方向不穩定 | Replay buffer 做隨機取樣，打破時序相關性 |
| Target 隨 θ 同步變動，訓練發散 | Target network 每隔 `sync_freq` 步才同步一次 |

**訓練流程：**

1. 以 ε-greedy 與環境互動，transition 存入 buffer
2. buffer 夠大時，隨機抽 `batch_size` 筆
3. 用 target network 算 Y，online network 算 X，計算 MSELoss
4. 每 `sync_freq` 步將 online 參數複製到 target

---

## 實驗設定

### naive_static

| 超參數 | 值 |
|--------|----|
| epochs | 1000 |
| γ (gamma) | 0.9 |
| ε start → end | 1.0 → 0.1 |
| lr | 0.001 |
| 環境模式 | static |

靜態地圖：每局 Player / Goal / Pit 位置固定，是最簡單的設定。

### replay_random

| 超參數 | 值 |
|--------|----|
| epochs | 5000 |
| γ (gamma) | 0.9 |
| ε | 0.3（固定） |
| lr | 0.001 |
| mem_size | 1000 |
| batch_size | 200 |
| max_moves | 50 |
| sync_freq | 500 |
| 環境模式 | random |

隨機地圖：每局重新隨機擺放所有棋子，狀態空間更大、更難泛化。

---

## 量化結果

| 實驗 | 勝率 | 平均步數（贏） | Loss mean±std（後 100 步） | 訓練時間 |
|------|------|--------------|--------------------------|----------|
| naive_static | **100%** | 7.0 步 | 0.00777 ± 0.00995 | 14.9 秒 |
| replay_random | **87.6%** | 2.6 步 | 0.0632 ± 0.0385 | 50.1 秒 |

---

## Loss 曲線

| naive_static | replay_random |
|:---:|:---:|
| ![naive_static loss curve](results/HW3-1/naive_static/loss.png) | ![replay_random loss curve](results/HW3-1/replay_random/loss.png) |

---

## 策略動畫

| naive_static | replay_random |
|:---:|:---:|
| ![naive_static agent animation](results/HW3-1/naive_static/dashboard.gif) | ![replay_random agent animation](results/HW3-1/replay_random/dashboard.gif) |

---

## 觀察與討論

**1. Naive DQN 在靜態地圖可以收斂**

靜態地圖只有一種初始局面，狀態空間極小（理論上只有幾十種不同的棋盤狀態）。即使 Naive DQN 訓練信號不穩定，只要 1000 epoch 就能達到 100% 勝率。Loss 呈現鋸齒狀下降（std > mean），反映 target 隨 online 參數飄移的不穩定性。

**2. Replay 讓隨機地圖可訓練**

隨機地圖每局棋盤不同，沒有 replay buffer 根本無法收斂——相鄰 step 的樣本來自同一局，梯度更新方向幾乎一樣，模型會過擬合當前局面。加入 replay buffer 後，minibatch 涵蓋歷史多局的 transition，梯度方向更「平均」，模型學到跨局通用的策略。

**3. 87.6% 已是不錯的起點**

random 模式下棋盤有更多可能排列，5000 epoch + batch 200 的設定已讓模型學到「避開 Pit、走向 Goal」的基本邏輯。剩餘的失敗局通常是 Pit 緊鄰 Goal 造成的幾何陷阱，需要更強的 Q-value 估計才能解決（後續 HW3-2 引入 Double + Dueling 處理）。
